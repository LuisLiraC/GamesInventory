@page "/warehouses/edit/{WarehouseId}"

<UpdateWarehouseComponent WarehouseId="@WarehouseId" />

@code {
    [Parameter]
    public string WarehouseId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        WarehouseId = WarehouseId;
    }
}